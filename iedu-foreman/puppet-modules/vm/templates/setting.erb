# Konfiguriert via Foreman, nicht direkt bearbeiten
network=<%=@config[@name]["network"]%>
macaddr=<%=@config[@name]["macaddr"]%>
bridgeadapter=<%=@config[@name]["bridgeadapter"]%>
usbmode=<%=@config[@name]["usbmode"]%>
accelerate2dvideo=<%=@config[@name]["accelerate2dvideo"]%>
accelerate3d=<%=@config[@name]["accelerate3d"]%>
monitorcount=<%=@config[@name]["monitorcount"]%>
